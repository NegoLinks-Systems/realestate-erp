# Deployment Guide

This file has been superseded by the complete, current guide at the repository root:

**➡ See `SETUP_GUIDE.md` in the project root.**

`SETUP_GUIDE.md` covers the full, up-to-date procedure for all modules (Phases 0–7):
tooling, creating the Supabase project, pushing all 18 migrations, storage buckets,
environment variables, bootstrapping the first admin, deploying to Vercel, the
invite edge function, custom domains via Cloudflare, and scheduling the nightly jobs
with pg_cron — plus a troubleshooting table and instructions for running a second
branded instance.
